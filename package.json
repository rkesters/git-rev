{
  "name": "@rkesters/git-agent",
  "version": "1.0.1",
  "description": "Synchronously get the current git commit hash, tag, or branch",
  "repository": "https://github.com/rkesters/git-rev",
  "license": "MIT",
  "author": "rkesters",
  "main": "./dist/index.js",
  "access": "public",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:watch": "rimraf dist && tsc --watch",
    "prepublishOnly": "npm run build",
    "test": "npx jest",
    "format": "npx prettier --write './src/**/*.ts'"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.29",
    "@types/jest": "^25.1.3",
    "@types/node": "^13.7.1",
    "bluebird": "^3.7.2",
    "jest": "^25.1.0",
    "moment": "^2.24.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.2.1",
    "ts-node": "^8.6.2",
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  },
  "engines": {
    "node": ">= 11"
  },
  "dependencies": {
    "@types/fs-extra": "^8.1.0",
    "fs-extra": "^8.1.0"
  }
}
