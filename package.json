{
  "name": "@rkesters/git-agent",
  "version": "1.0.0",
  "description": "Synchronously get the current git commit hash, tag, or branch",
  "repository": "https://github.com/rkesters/git-rev",
  "license": "MIT",
  "author": "rkesters",
  "main": "./dist/index.js",
  "access": "public",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:watch": "rimraf dist && tsc --watch",
    "prepublishOnly": "npm run build",
    "test": ". ./tests/setup.sh && ts-node ./tests/index.ts && rm -Rf ./tempRepo"
  },
  "devDependencies": {
    "@types/node": "^13.7.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.6.2",
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  },
  "engines": {
    "node": ">= 11"
  }
}
